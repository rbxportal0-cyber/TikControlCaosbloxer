<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <ProjectReference Include="..\TikControl.Core\TikControl.Core.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Web.WebView2" Version="1.0.4191.47" />
  </ItemGroup>
  <!-- Interfaz web: se copia a build/out bajo "web/" y WebView2 la sirve por VirtualHost.
       También se EMBEBE como recurso para que el exe sea autocontenido: al arrancar,
       la app extrae el diseño al disco, de modo que actualizar el exe actualiza el diseño. -->
  <ItemGroup>
    <Content Include="..\TikControl.Web\**\*" Link="web\%(RecursiveDir)%(Filename)%(Extension)" CopyToOutputDirectory="PreserveNewest" />
    <EmbeddedResource Include="..\TikControl.Web\**\*" Link="web\%(RecursiveDir)%(Filename)%(Extension)" LogicalName="TikControl.Web.%(RecursiveDir)%(Filename)%(Extension)" />
  </ItemGroup>
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <AssemblyName>TikControlCaosbloxer</AssemblyName>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <Version>1.0.3</Version>
    <Authors>sigato</Authors>
    <Product>TikControl Caosbloxer</Product>
    <AssemblyVersion>1.0.0.0</AssemblyVersion>
    <FileVersion>1.0.0.0</FileVersion>
  </PropertyGroup>
</Project>